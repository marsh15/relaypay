# RelayPay v1.0.0 release evidence

## Published release

- Implementation PR: https://github.com/marsh15/relaypay/pull/31
- Implementation PR CI: https://github.com/marsh15/relaypay/actions/runs/36251255460 (`success`)
- Canonical post-merge CI: https://github.com/marsh15/relaypay/actions/runs/36251599846
  (`success`)
- Green `main` SHA: `3bb73947530fccc742c08f6adff6001f39556f1b`
- Annotated tag object SHA: `87466b06d1e7e176ac26e25678e784f52e29205e`
- Tag target SHA: `3bb73947530fccc742c08f6adff6001f39556f1b`
- Public release: https://github.com/marsh15/relaypay/releases/tag/v1.0.0
- Release-note SHA-256: `6b48467a76da0c95b6bdc3aef572d8a74a120e2e778ee9247b9cc0984eea2a0c`
- Repository visibility/license: public / MIT
- Binary assets: intentionally empty
- Release warning: synthetic INR portfolio software; no real money, identity, merchant, banking,
  messaging, website, or payment-rail data; deterministic synthetic fixtures only; no deployment
  or hosting. The live-provider comparison script is release-only, inert without vendor API keys,
  and never runs in CI.

Required evidence includes the empty and all m1–m14 upgrade paths, all five Alembic drift checks,
the full backend and permanent lost-response gates, the versioned v1.0.0 deterministic evaluation
suite (134 fixtures: disputes, recovery, settlement including all four intents and clarifications,
merchant risk, and adversarial cases with ≥0.90 evidence-retrieval precision and zero policy,
numeric, citation, escalation, tenant, PII, injection, or duplicate-effect violations, recorded as
`EvaluationDataset`/`EvaluationRun`), the portfolio analytics gauges and Grafana panels, console
lint/type/build/audit, Playwright/axe journeys, Compose recreation, the public MIT license, the
synthetic-data warning, and intentionally empty binary assets.

## Local pre-publish gate — 2026-09-26

- Upgrade proofs (`verify_m1_upgrade` … `verify_m14_upgrade`): all thirteen pass in CI order on a
  fresh database; every earlier release's evidence stayed unchanged and each later module's tables
  started empty.
- Alembic drift: no new upgrade operations detected for RelayPay, provider, receiver, bank, or
  commerce. v1.0.0 adds no database migration — the analytics layer is query-only.
- Backend: Ruff and formatting clean across 271 files; strict mypy clean over 190 source files;
  221 backend tests pass against a freshly migrated database, including the new provider-router
  fallback-exhaustion, terminal-no-fallback, and live-provider report aggregation tests.
- Deterministic evaluation gate: `python -m scripts.run_evaluations` prints
  `V1 evaluation PASS: all deterministic thresholds hold` — 30 dispute, 30 recovery, 24
  settlement, 30 risk, and 20 adversarial cases replayed through the real domain services with the
  deterministic providers; results recorded as `EvaluationDataset`/`EvaluationRun`.
- Compatibility: frozen `/api/v1` compatibility, generated SDK drift, and exact-byte webhook
  verification passed after regenerating the OpenAPI snapshots and Python SDK for the new
  portfolio analytics endpoint.
- Console: ESLint, TypeScript, the Next.js production build, and the high-severity dependency
  audit passed with zero reported vulnerabilities.
- Browser: all six Playwright journeys (lost-response, operations tour, disputes, recovery,
  settlement, and risk review) passed with axe checks at desktop and 320×900 against a clean
  recreated Compose stack including the `merchant-site` service; the in-container migrate service
  performed the migration and seed.
- Edge: lint, typecheck, tests, dry-run build, and dependency audit passed with zero reported
  vulnerabilities.
- Compose: development, observability, and production graph validation passed.
- Portfolio demo: `make portfolio-demo` completes the end-to-end synthetic journey (payment
  authorization and capture through the deterministic provider, settlement question, recovery case
  with scheduled actions, dispute draft, risk review, portfolio analytics summary).
- Binary assets: intentionally empty for v1.0.0.

The release gate passed on the first run for this milestone: the implementation PR CI and the
canonical post-merge release gate were both green on the exact annotated-tag target. No release
gate was waived or retried.
