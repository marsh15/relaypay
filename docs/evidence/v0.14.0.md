# RelayPay v0.14.0 release evidence

## Published release

- Implementation PR: https://github.com/marsh15/relaypay/pull/27
- Implementation PR CI: https://github.com/marsh15/relaypay/actions/runs/36175687063 (`success`)
- Canonical post-merge CI: https://github.com/marsh15/relaypay/actions/runs/36176317911
  (`success`)
- Green `main` SHA: `87e20fd1bf955b88c5aa8ce8fdcc0150bc7dc716`
- Annotated tag object SHA: `7f686cab28734d24f14cb5aed1bf7d3a04f7f305`
- Tag target SHA: `87e20fd1bf955b88c5aa8ce8fdcc0150bc7dc716`
- Public release: https://github.com/marsh15/relaypay/releases/tag/v0.14.0
- Release-note SHA-256: `07c03c5959ecb2ce28e7f5841c3dd55e9487914f947e74e8d25123a41d340b2d`
- Repository visibility/license: public / MIT
- Binary assets: intentionally empty
- Release warning: synthetic INR portfolio software; no real money, identity, customer, banking,
  messaging, or payment-rail data; deterministic mock providers and a deterministic fake model
  provider in CI; no deployment or hosting.

Required evidence includes the empty and v0.13 upgrade paths, all five Alembic drift checks, the
full backend and permanent lost-response gates, the deterministic settlement question suite
(timezone/cutoff boundaries, T+0/T+1/T+2 policies, weekend include/skip, forecast immutability,
100% numeric agreement, complete citations, typed clarification, prompt-injection content,
tenant/environment isolation, idempotent questions and workflow replay), console
lint/type/build/audit, Playwright/axe journeys, Compose recreation, the public MIT license, the
synthetic-data warning, and intentionally empty binary assets.

## Local pre-publish gate — 2026-09-25

- v0.13 upgrade proof (`verify_m13_upgrade`): passed; v0.13 authoritative rows were unchanged and
  all four settlement tables started empty. All eleven earlier upgrade proofs passed in CI order on
  a fresh database.
- Alembic drift: no new upgrade operations for RelayPay, provider, receiver, bank, or commerce.
- Backend: Ruff and formatting passed for 249 files; strict mypy passed for 174 source files; all
  181 tests passed (52 unit, 129 integration) including the settlement window matrix, the
  classification/injection suite, narrative validation, and the eight end-to-end settlement
  integration proofs.
- Compatibility: frozen `/api/v1` compatibility, generated SDK drift, and exact-byte webhook
  verification passed after regenerating the OpenAPI snapshot and Python SDK for the new admin
  endpoints.
- Console: ESLint, TypeScript, and the Next.js production build passed; the required high-severity
  dependency audit passed after bumping Next.js 16.2.11 → 16.3.6 and the postcss/sharp overrides
  to 8.5.23/0.35.4 in response to newly published critical/high advisories.
- Browser: all five Playwright journeys (including the two settlement journeys) passed with axe
  checks against the complete local Compose stack.
- Edge: lint, typecheck, tests, dry-run build, and dependency audit passed.
- Compose: development, observability, and production graph validation passed; the full
  observability stack was recreated cleanly from an empty volume with the in-container migrate and
  seed services.
- Binary assets: intentionally empty for v0.14.0.

Two implementation PR CI runs failed only in the browser step before the root causes were found and
fixed: the console backend proxy dropped the `Idempotency-Key` header (settlement question POSTs
were rejected as validation errors), and the settlement e2e asserted page text that JSX rendered as
separate text nodes. A third local run exposed the API login rate limiter when six journeys logged
in within one minute; the settlement journeys were consolidated into one logged-in test. The final
implementation PR release gate passed. The canonical post-merge release gate passed on the exact
annotated-tag target. No release gate was waived.
